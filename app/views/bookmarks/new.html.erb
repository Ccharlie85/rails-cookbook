<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= asset_path "banner.jpg" %>);">
  <div class="container">
  <h1>Which receipe would you like to add to your <%= @category.name %> list?</h1>
  </div>
</div>

  <%= simple_form_for[@category, @bookmark] do |f| %>
    <%= f.input :comment, label: "Category Name" %>
    <%= f.association :recipe %>
    <%= f.button :submit, value: "Add Recipe" %>
  <% end %>

  <%= link_to "Back", category_path(@category) %>
</body>
</html>
